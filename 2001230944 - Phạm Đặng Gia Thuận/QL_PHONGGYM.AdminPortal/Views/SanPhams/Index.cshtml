@model IEnumerable<QL_PHONGGYM.AdminPortal.Models.SanPham>
@{
    ViewBag.Title = "Quản Lý Sản Phẩm";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .container-custom {
        padding: 30px;
        background: #f8f9fa;
        min-height: 100vh;
    }

    .page-header {
        background: #5dade2;
        padding: 30px;
        border-radius: 10px;
        margin-bottom: 30px;
        box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    .page-header h2 {
        color: white;
        margin: 0;
        font-weight: 500;
        font-size: 26px;
    }

    .btn-create {
        background: #5dade2;
        color: white;
        padding: 10px 25px;
        border-radius: 5px;
        text-decoration: none;
        font-weight: 500;
        display: inline-block;
        transition: all 0.3s;
        border: none;
    }

    .btn-create:hover {
        background: #3498db;
        color: white;
        text-decoration: none;
    }

    .table-container {
        background: white;
        border-radius: 10px;
        padding: 25px;
        box-shadow: 0 2px 5px rgba(0,0,0,0.08);
        overflow-x: auto;
    }

    .table-custom {
        width: 100%;
        margin: 0;
    }

    .table-custom thead {
        background: #5dade2;
    }

    .table-custom thead th {
        color: white;
        font-weight: 500;
        padding: 15px;
        border: none;
        text-align: left;
        font-size: 14px;
        white-space: nowrap;
    }

    .table-custom tbody tr {
        border-bottom: 1px solid #e9ecef;
        transition: all 0.3s;
    }

    .table-custom tbody tr:hover {
        background: #f0f8ff;
    }

    .table-custom tbody td {
        padding: 15px;
        vertical-align: middle;
        font-size: 14px;
    }

    .action-links a {
        padding: 6px 15px;
        border-radius: 20px;
        text-decoration: none;
        font-size: 13px;
        font-weight: 500;
        transition: all 0.3s;
        display: inline-block;
        margin: 2px;
    }

    .action-links .btn-edit {
        background: #4CAF50;
        color: white;
    }

    .action-links .btn-edit:hover {
        background: #45a049;
    }

    .action-links .btn-details {
        background: #2196F3;
        color: white;
    }

    .action-links .btn-details:hover {
        background: #0b7dda;
    }

    .action-links .btn-delete {
        background: #f44336;
        color: white;
    }

    .action-links .btn-delete:hover {
        background: #da190b;
    }

    .price {
        color: #e74c3c;
        font-weight: 600;
    }

    .stock {
        padding: 5px 12px;
        border-radius: 15px;
        font-size: 12px;
        font-weight: 600;
        background: #d4edda;
        color: #155724;
    }

    .description {
        max-width: 200px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    @@media (max-width: 768px) {
        .table-container {
            padding: 15px;
        }

        .page-header {
            padding: 20px;
        }

        .page-header h2 {
            font-size: 22px;
        }
    }
</style>

<div class="container-custom">
    <div class="page-header">
        <h2>📦 Quản Lý Sản Phẩm</h2>
    </div>

    <div style="margin-bottom: 20px;">
        @Html.ActionLink("➕ Thêm Sản Phẩm Mới", "Create", null, new { @class = "btn-create" })
    </div>

    <div class="table-container">
        <table class="table-custom">
            <thead>
                <tr>
                    <th>Loại Sản Phẩm</th>
                    <th>Tên Sản Phẩm</th>
                    <th>Đơn Giá</th>
                    <th>Số Lượng Tồn</th>
                    <th>Giá KM</th>
                    <th>Hãng</th>
                    <th>Xuất Xứ</th>
                    <th>Bảo Hành</th>
                    <th>Mô Tả</th>
                    <th>Thao Tác</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.LoaiSanPham.TenLoaiSP</td>
                        <td><strong>@item.TenSP</strong></td>
                        <td class="price">@String.Format("{0:N0}đ", item.DonGia)</td>
                        <td><span class="stock">@item.SoLuongTon</span></td>
                        <td class="price">@(item.GiaKhuyenMai.HasValue ? String.Format("{0:N0}đ", item.GiaKhuyenMai) : "-")</td>
                        <td>@item.Hang</td>
                        <td>@item.XuatXu</td>
                        <td>@item.BaoHanh</td>
                        <td class="description" title="@item.MoTaChung">@item.MoTaChung</td>
                        <td class="action-links">
                            @Html.ActionLink("Sửa", "Edit", new { id = item.MaSP }, new { @class = "btn-edit" })
                            @Html.ActionLink("Chi tiết", "Details", new { id = item.MaSP }, new { @class = "btn-details" })
                            @Html.ActionLink("Xóa", "Delete", new { id = item.MaSP }, new { @class = "btn-delete" })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>